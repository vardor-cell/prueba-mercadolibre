ipython>=8.10
jupyterlab>=3.0
notebook
kedro[jupyter]~=1.4.0
kedro-datasets>=9.1
kedro-viz>=6.7.0
scikit-learn~=1.5.1; python_version <= '3.10'
scikit-learn~=1.8.0; python_version > '3.10'

# BigQuery (datos en la nube)
pandas-gbq>=0.23
google-cloud-bigquery>=3.25
db-dtypes>=1.3
pyarrow>=16.0
tqdm>=4.66

# Testing
pytest>=8.0
requests>=2.31

# Notebooks: gráficos interactivos (los notebooks de notebooks/ usan Plotly).
plotly>=5.24

# NOTA: este requirements.txt es el CORE (pipeline + notebooks) y es el que usa la
# imagen Docker del pipeline (Dockerfile raíz) — por eso se mantiene liviano, sin
# FastAPI ni Streamlit. Para un entorno local ÚNICO con TODO (API + dashboard
# incluidos) usá `requirements-dev.txt` (o `make install`).
